# Интеграция с MultiTransfer.ru

## Задача

Добавить поддержку создания платежей через новый сайт https://multitransfer.ru/

## Структура проекта

```
bot/
├── multitransfer_service.py  # Новый модуль для работы с multitransfer.ru
├── payment_service.py         # Существующий модуль для elecsnet.ru
├── browser_manager.py         # Менеджер браузеров
└── ...

test_multitransfer.py          # Локальный тест для multitransfer.ru
```

## Локальное тестирование

### 1. Установка зависимостей

```bash
pip install -r bot/requirements.txt
```

### 2. Запуск теста

```bash
python test_multitransfer.py
```

Тест откроет браузер с сайтом multitransfer.ru и позволит:
- Изучить структуру страницы
- Найти нужные элементы формы
- Протестировать создание платежа

### 3. Изучение сайта

При запуске теста нужно:

1. **Изучить форму создания платежа:**
   - Какие поля нужно заполнить?
   - Какие селекторы у полей (name, id, class)?
   - Есть ли авторизация?

2. **Найти элементы:**
   - Поле суммы
   - Поле номера карты
   - Поле имени владельца
   - Кнопка создания платежа

3. **Изучить результат:**
   - Где появляется ссылка на оплату?
   - Где QR код?
   - Какой формат ссылки?

## Реализация

После изучения сайта нужно обновить `bot/multitransfer_service.py`:

```python
def create_payment(self, amount, card_number, owner_name):
    # 1. Найти форму
    amount_input = wait.until(EC.presence_of_element_located((By.NAME, "amount")))
    
    # 2. Заполнить поля
    amount_input.send_keys(str(amount))
    card_input.send_keys(card_number)
    name_input.send_keys(owner_name)
    
    # 3. Нажать кнопку
    submit_btn.click()
    
    # 4. Получить результат
    payment_link = ...
    qr_code = ...
    
    return {
        "payment_link": payment_link,
        "qr_base64": qr_code,
        "success": True
    }
```

## Интеграция в API

После успешного локального тестирования нужно:

1. Обновить `bot/admin_panel.py` для поддержки выбора провайдера
2. Добавить endpoint для multitransfer
3. Обновить базу данных для хранения настроек

## Следующие шаги

- [ ] Локальное тестирование
- [ ] Изучение структуры multitransfer.ru
- [ ] Реализация логики создания платежа
- [ ] Интеграция в API
- [ ] Тестирование на сервере
- [ ] Деплой

## Примечания

- Сначала все тестируем локально
- Браузер открывается в видимом режиме для отладки
- После успешного теста можно включить headless режим
- Логика может отличаться от elecsnet.ru

---

**Статус:** 🚧 В разработке  
**Дата:** 16 января 2026
