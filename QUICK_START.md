# 🚀 Быстрый старт

## Вариант 1: Простой режим (без Playwright)

```bash
# 1. Установка
pip install -r admin/requirements.txt

# 2. Запуск
python start_admin.py

# 3. Открыть
# http://localhost:5000 - админка
# http://localhost:5001 - API
```

## Вариант 2: Полный режим (с Playwright)

```bash
# 1. Установка
pip install -r admin/requirements.txt
pip install -r requirements_playwright.txt

# 2. Установка браузера
playwright install chromium

# 3. Запуск
python start_admin.py

# 4. Открыть
# http://localhost:5000 - админка
# http://localhost:5001 - API (с реальной генерацией)
```

## 🧪 Тестирование

```bash
# Проверка API
python test_api.py

# Проверка режима работы
curl http://localhost:5001/health
```

Ответ покажет:
- `"mode": "playwright"` - реальная генерация через браузер ✅
- `"mode": "proxy"` - простое перенаправление ⚠️

## 📝 Примечания

- **База данных**: `admin/linkflow.db` (SQLite)
- **API Token**: `-3uVLlbWyy90eapOGkv70C2ZltaYTxq-HtDbq-DtlLo`
- **Продакшн**: `85.192.56.74:5000` и `85.192.56.74:5001`
- **Данные отправителя**: `src/sender_data.py`

## 🔄 Синхронизация с сервером

```bash
# Скачать БД с сервера
scp root@85.192.56.74:/root/linkflow-admin/linkflow.db admin/

# Скачать payment_service
scp root@85.192.56.74:/root/LinkFlow_playwright/payment_service.py ./

# Загрузить БД на сервер
scp admin/linkflow.db root@85.192.56.74:/root/linkflow-admin/
```

## ⚡ Разница между режимами

| Функция | Proxy режим | Playwright режим |
|---------|-------------|------------------|
| Установка | Быстрая | Требует Playwright |
| Генерация платежей | Через админку | Через браузер |
| Скорость | Быстро | Медленнее (~30-60 сек) |
| Реальность | Демо | Реальные платежи |
