# Быстрый Старт

## Запуск Бота

```bash
cd bot
python bot.py
```

Бот автоматически:
1. Проверит аккаунты elecsnet
2. Прогреет браузер
3. Будет готов к созданию платежей за 3-5 секунд

## Создание Платежа

1. Открыть бота в Telegram
2. Нажать "💳 Создать ссылку"
3. Ввести сумму (1000-100000 руб)
4. Получить ссылку и QR код за 3-5 секунд

## Режимы Работы

### SELENIUM (По умолчанию) ✅
- Скорость: 3-5 секунд
- Надежность: Высокая
- Рекомендуется для использования

### HYBRID (Экспериментальный) ⚠️
- Целевая скорость: 1-2 секунды
- Статус: В разработке, не работает
- Автоматически переключается на SELENIUM при ошибках

## Переключение Режимов

### Через Telegram:
1. "📊 Админ" → "🔧 Режим создания"
2. Выбрать режим

### Через Код:
```python
from payment_modes import mode_manager, PaymentMode

# SELENIUM (рекомендуется)
mode_manager.set_mode(PaymentMode.SELENIUM)

# HYBRID (экспериментальный)
mode_manager.set_mode(PaymentMode.HYBRID)
```

## Тестирование

### Тест SELENIUM режима:
```bash
python test_payment.py
```

### Тест HYBRID режима:
```bash
python test_hybrid_mode.py
```

## Проблемы и Решения

### Chrome крашится
```bash
# Убить все процессы Chrome
taskkill /F /IM chrome.exe

# Перезапустить бота
python bot.py
```

### Браузер не прогревается
- Проверить аккаунты: "📊 Админ" → "👥 Аккаунты"
- Проверить реквизиты: "📊 Админ" → "💳 Реквизиты"

### Медленное создание платежей
- Убедиться что используется SELENIUM режим (по умолчанию)
- Проверить что браузер прогрет: смотреть логи при запуске

## Документация

- `README.md` - полная документация
- `CHANGELOG.md` - история изменений
- `HYBRID_MODE_STATUS.md` - статус гибридного режима
- `SUMMARY.md` - итоговый отчет

## Поддержка

При проблемах проверить:
1. Chrome установлен и доступен
2. Аккаунты elecsnet работают
3. Реквизиты добавлены
4. Используется SELENIUM режим (по умолчанию)
