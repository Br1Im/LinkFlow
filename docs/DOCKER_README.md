# 🐳 Docker для тестирования MultiTransfer.ru

## 🚀 Быстрый старт

### 1. Запуск теста

```bash
./run_test.sh
```

Это запустит автоматический тест с тестовыми данными:
- Карта: 9860080323894719
- Владелец: Nodir Asadullayev
- Сумма: 1000 руб.

### 2. Интерактивный режим

```bash
./run_interactive.sh
```

Откроет bash внутри контейнера, где можно:

```bash
# Запустить тест
python test_multitransfer.py

# Запустить payment_manager
python payment_manager.py

# Посмотреть скриншоты
ls -la screenshots/
```

## 📁 Структура

```
LinkFlow/
├── Dockerfile              # Конфигурация Docker образа
├── docker-compose.yml      # Конфигурация сервисов
├── multitransfer_payment.py # Модуль для multitransfer.ru
├── payment_manager.py      # Главный менеджер
├── test_multitransfer.py   # Тестовый скрипт
├── run_test.sh            # Скрипт запуска теста
├── run_interactive.sh     # Скрипт интерактивного режима
└── screenshots/           # Скриншоты (создается автоматически)
```

## 🔧 Что внутри Docker

- **Python 3.11**
- **Google Chrome** (stable)
- **ChromeDriver** (автоматически подбирается версия)
- **Xvfb** (виртуальный дисплей для headless режима)
- **Selenium** и все зависимости

## 📸 Скриншоты

После запуска теста скриншоты сохраняются в `./screenshots/`:

- `success.png` - при успешном создании платежа
- `error.png` - при ошибке
- `critical_error.png` - при критической ошибке

## 🛠️ Ручные команды Docker

### Сборка образа
```bash
docker-compose build
```

### Запуск теста
```bash
docker-compose up
```

### Остановка контейнера
```bash
docker-compose down
```

### Просмотр логов
```bash
docker-compose logs
```

### Очистка
```bash
docker-compose down -v
docker system prune -a
```

## 🐛 Отладка

### Проблема: ChromeDriver не найден

Пересоберите образ:
```bash
docker-compose build --no-cache
```

### Проблема: Ошибка подключения к Chrome

Проверьте что Xvfb запущен:
```bash
docker-compose run --rm multitransfer-test ps aux | grep Xvfb
```

### Проблема: Скриншоты не сохраняются

Проверьте права на папку:
```bash
chmod 777 screenshots/
```

## 📊 Логи

Все логи выводятся в консоль с детальной информацией:
- 🔧 Инициализация
- 📌 Каждый шаг процесса
- ✅ Успешные операции
- ⚠️ Предупреждения
- ❌ Ошибки

## 🎯 Преимущества Docker

✅ Изолированная среда
✅ Одинаковое окружение на всех машинах
✅ Не нужно устанавливать Chrome локально
✅ Автоматическая настройка ChromeDriver
✅ Легко масштабировать
✅ Простое развертывание на сервере
