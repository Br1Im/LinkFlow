# 🏗️ Архитектура проекта

## 📐 Структура

```
LinkFlow/
├── src/                          # Исходный код приложения
│   ├── __init__.py              # Инициализация пакета
│   ├── config.py                # Конфигурация (URL, таймауты, константы)
│   ├── multitransfer_payment.py # Основной модуль автоматизации
│   └── payment_manager.py       # Высокоуровневый менеджер
│
├── tests/                        # Тесты
│   ├── __init__.py
│   ├── test_multitransfer.py    # Тест для Docker (headless)
│   └── test_local.py            # Локальный тест (с UI)
│
├── scripts/                      # Скрипты запуска
│   ├── run_test.sh              # Запуск Docker теста
│   └── run_interactive.sh       # Интерактивный Docker режим
│
├── docs/                         # Документация
│   ├── README.md                # Основная документация
│   ├── DOCKER_README.md         # Docker документация
│   └── ARCHITECTURE.md          # Этот файл
│
├── screenshots/                  # Скриншоты для отладки
│   └── .gitkeep
│
├── Dockerfile                    # Docker образ
├── docker-compose.yml            # Docker Compose конфигурация
├── requirements.txt              # Python зависимости
├── setup.py                      # Установка как пакет
├── .gitignore                    # Git ignore правила
└── README.md                     # Главный README
```

## 🔧 Компоненты

### 1. src/config.py
Централизованная конфигурация:
- URL сервисов
- Таймауты
- Тестовые данные
- Константы

### 2. src/multitransfer_payment.py
Основной модуль автоматизации:
- `MultitransferPayment` класс
- Управление браузером (Selenium)
- Логика создания платежей
- Обработка ошибок

**Методы:**
- `login()` - инициализация браузера
- `create_payment()` - создание платежа
- `close()` - закрытие браузера

### 3. src/payment_manager.py
Высокоуровневый менеджер:
- `PaymentManager` класс
- Упрощенный API
- Управление жизненным циклом

**Методы:**
- `initialize()` - инициализация
- `create_payment()` - создание платежа
- `close()` - очистка ресурсов

### 4. tests/test_multitransfer.py
Docker тест:
- Headless режим
- Автоматический запуск
- Сохранение скриншотов
- Детальное логирование

### 5. tests/test_local.py
Локальный тест:
- Видимый браузер
- Интерактивный режим
- Отладка

## 🔄 Поток выполнения

```
1. Инициализация
   ├── Создание Chrome драйвера
   ├── Настройка опций (headless/visible)
   └── Открытие multitransfer.ru

2. Выбор страны
   ├── Клик на блок выбора страны
   └── Выбор Узбекистана

3. Ввод суммы
   ├── Поиск поля ввода
   ├── Ввод суммы
   └── Подтверждение (Enter)

4. Ожидание списка банков
   ├── Проверка появления банков
   └── Таймаут 60 секунд

5. Выбор банка
   ├── Поиск Uzcard/Humo
   └── Клик на банк

6. Заполнение формы
   ├── Ввод номера карты
   ├── Ввод имени владельца
   └── Отправка формы

7. Получение результата
   ├── Поиск QR-кода
   ├── Поиск ссылки
   └── Возврат результата

8. Очистка
   └── Закрытие браузера
```

## 🐳 Docker архитектура

```
Dockerfile
├── Base: python:3.11-slim
├── Системные зависимости
│   ├── Chrome
│   ├── ChromeDriver
│   └── Xvfb (виртуальный дисплей)
├── Python зависимости
│   └── selenium
└── Копирование кода
    ├── src/
    ├── tests/
    └── requirements.txt

docker-compose.yml
├── Service: multitransfer-test
├── Volumes
│   ├── screenshots (для сохранения)
│   ├── src (для разработки)
│   └── tests (для разработки)
└── Environment
    ├── DISPLAY=:99
    └── PYTHONUNBUFFERED=1
```

## 📦 Зависимости

```
selenium>=4.0.0  # Автоматизация браузера
```

## 🔐 Безопасность

- Нет хранения паролей
- Изолированная среда Docker
- Временные скриншоты
- Автоматическая очистка

## 🚀 Расширение

### Добавление нового провайдера

1. Создать `src/new_provider_payment.py`
2. Реализовать методы `login()` и `create_payment()`
3. Добавить в `src/payment_manager.py`
4. Создать тест в `tests/test_new_provider.py`

### Добавление новой функции

1. Обновить `src/multitransfer_payment.py`
2. Добавить конфигурацию в `src/config.py`
3. Написать тест
4. Обновить документацию

## 📊 Метрики

- Время создания платежа: 40-60 сек
- Размер Docker образа: ~1.5 GB
- Потребление памяти: ~500 MB
- CPU: 1-2 ядра

## 🎯 Принципы

1. **Модульность** - каждый компонент независим
2. **Тестируемость** - все покрыто тестами
3. **Документированность** - подробная документация
4. **Изолированность** - Docker для изоляции
5. **Простота** - минимальный код
