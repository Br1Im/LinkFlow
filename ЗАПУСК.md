# 🚀 Запуск LinkFlow

## Админка запущена!

✅ **URL**: http://localhost:5000

## Что делать дальше:

1. **Откройте в браузере**: http://localhost:5000

2. **Создайте платёж**:
   - Выберите платёжную систему (Multitransfer/Elecsnet)
   - Выберите режим (standard/fast/test)
   - Номер карты и имя владельца уже заполнены из config.py
   - Введите сумму: `500`
   - Нажмите "Создать платёж"

3. **Подождите ~30-40 секунд**
   - Платёж создаётся в headless режиме
   - Страна и банк выбираются автоматически при логине
   - Экономия времени: ~10-20 секунд

4. **Проверьте результат**:
   - Ссылка на платёж
   - Время выполнения
   - Статус

## Логи Docker

```bash
# В другом терминале
sudo docker-compose -f LinkFlow/docker-compose.local.yml logs -f
```

## Остановка

```bash
sudo docker-compose -f LinkFlow/docker-compose.local.yml down
```

## Тестирование API

```bash
# Создать платёж
curl -X POST http://localhost:5000/api/create-payment \
  -H "Content-Type: application/json" \
  -d '{
    "card_number": "9860080323894719",
    "owner_name": "Test User",
    "amount": 500,
    "payment_mode": "test",
    "payment_system": "multitransfer"
  }'

# Проверить статус (замените 1 на ID платежа)
curl http://localhost:5000/api/payment/1

# Список всех платежей
curl http://localhost:5000/api/payments
```

## Оптимизация

✅ **Страна выбирается при логине** (Узбекистан)
✅ **Банк выбирается при логине** (Uzcard/Humo)
✅ **При создании платежа вводится только сумма**
⚡ **Экономия: ~10-20 секунд на платеж**

## Режимы платежей

| Режим | Лимиты (RUB) |
|-------|--------------|
| standard | 110 - 120,000 |
| fast | 500 - 50,000 |
| test | 110 - 1,000 |

---

**Статус**: ✅ Работает!
