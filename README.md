# 🎯 Полностью автоматический API для multitransfer.ru

Автоматическое получение токенов + решение капчи + создание QR-ссылок.

## 📁 Файлы

- **`multitransfer_api.py`** - основной API класс
- **`auto_token_generator.py`** - автоматический генератор токенов
- **`fully_automatic_api.py`** - полностью автоматический API
- **`example.py`** - пример ручного использования
- **`requirements.txt`** - зависимости
- **`README.md`** - эта инструкция

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Полная автоматизация (РЕКОМЕНДУЕТСЯ)

```python
from fully_automatic_api import FullyAutomaticAPI

# Создаем полностью автоматический API
auto_api = FullyAutomaticAPI(headless=True)

# Создаем QR-ссылку (токен получается автоматически)
qr_link = auto_api.create_qr_payment(
    card_number="9860080323894719",
    recipient_name="Nodir Asadullayev", 
    amount=110
)

print(qr_link)  # https://qr.nspk.ru/...
```

### 3. Ручной режим (если нужен контроль)

1. Получи токен из браузера:
   - Открой https://multitransfer.ru/transfer/uzbekistan
   - F12 → Network
   - Заполни форму и реши капчу
   - Найди запрос к `api.multitransfer.ru`
   - Скопируй `fhptokenid` из заголовков

2. Используй API:
```python
from multitransfer_api import MultitransferAPI

api = MultitransferAPI("твой_токен")
qr_link = api.create_qr_payment("9860080323894719", "Nodir Asadullayev", 110)
```

## 🔗 API методы

### MultitransferAPI (базовый)
- `get_commissions(amount)` - получает commission_id
- `create_payment(commission_id, card, name)` - создает платеж  
- `get_qr_link(transaction_id)` - получает QR-ссылку
- `create_qr_payment(card, name, amount)` - полный процесс

### FullyAutomaticAPI (автоматический)
- `create_qr_payment(card, name, amount)` - создает QR с автоматическим токеном
- `create_multiple_payments(payments_list)` - массовое создание платежей

### AutoTokenGenerator (генератор токенов)
- `get_fresh_token()` - получает свежий токен автоматически

## 🤖 Автоматизация

### Решение капчи
- ✅ **Checkbox капча** - решается автоматически
- ❌ **Визуальная капча** - требует ручного решения
- 🔄 **Автообновление токенов** - каждые 25 минут

### Массовые платежи
```python
payments = [
    {"card": "9860080323894719", "name": "User 1", "amount": 110},
    {"card": "9860080323894719", "name": "User 2", "amount": 200}
]

results = auto_api.create_multiple_payments(payments)
```

## 💡 Важно

- **Минимальная сумма**: 110 RUB
- **Токен живет**: ~25 минут (автообновление)
- **Результат**: `https://qr.nspk.ru/...`
- **Капча**: checkbox решается автоматически
- **Chrome**: требуется для автоматизации

## 🎯 Примеры запуска

```bash
# Полная автоматизация
python3 fully_automatic_api.py

# Ручной режим
python3 example.py

# Только генерация токена
python3 auto_token_generator.py
```

Полная автоматизация готова! 🎉