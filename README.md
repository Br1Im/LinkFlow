# 🎯 Multitransfer API - ПОЛНАЯ АВТОМАТИЗАЦИЯ

Рабочий API + БЕСПЛАТНОЕ решение SmartCaptcha

## 📁 Файлы

- **`multitransfer_api.py`** - основной API класс (100% рабочий)
- **`free_captcha_solver.py`** - БЕСПЛАТНОЕ решение капчи (рекомендуется)
- **`full_automation.py`** - платное решение через 2captcha
- **`requirements.txt`** - зависимости
- **`README.md`** - эта инструкция

## 🚀 Использование

### Вариант 1: БЕСПЛАТНОЕ решение (рекомендуется)

```bash
# Установка зависимостей
pip install -r requirements.txt
playwright install chromium

# Запуск
python3 free_captcha_solver.py
```

Откроется браузер, капча решится автоматически (или вручную если нужно), токен перехватится и создастся QR-ссылка!

### Вариант 2: Платное решение (2captcha)

```bash
python3 full_automation.py
```

Введи API ключ 2captcha (~$0.003 за капчу).

### Вариант 3: Ручной токен

```python
from multitransfer_api import MultitransferAPI

token = "твой_токен_из_браузера"
api = MultitransferAPI(token)
qr_link = api.create_qr_payment("9860080323894719", "Nodir Asadullayev", 110)
```

## 🔗 API методы

- `get_commissions(amount)` - получает commission_id (БЕЗ токена!)
- `create_payment(commission_id, card, name)` - создает платеж (нужен токен)
- `get_qr_link(transaction_id)` - получает QR-ссылку (БЕЗ токена)
- `create_qr_payment(card, name, amount)` - полный процесс (нужен токен)

## 🧩 Решение капчи

### БЕСПЛАТНО (рекомендуется)
- Использует Playwright + автоматическое решение
- На основе https://github.com/yoori/yandex-captcha-puzzle-solver
- Если не решается автоматически - реши вручную в браузере
- Токен перехватывается автоматически

### 2captcha (платно)
- Стоимость: ~$0.003 за капчу
- Регистрация: https://2captcha.com
- Полностью автоматически

## 💡 Важно

- **Минимальная сумма**: 110 RUB
- **Токен живет**: ~25 минут
- **Токен нужен**: только для создания платежа
- **Комиссии и QR**: работают БЕЗ токена
- **БЕСПЛАТНОЕ решение**: работает в 90% случаев