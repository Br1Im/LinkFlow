# LinkFlow - Headless Payment Link Generator

🚀 **Полностью автоматизированная система создания платёжных ссылок, работающая в headless режиме как часы!**

## 🎯 Особенности

- ✅ **Headless режим** - работает без визуального интерфейса на серверах без монитора
- ✅ **Быстрая работа** - создание платежа за ~20 секунд
- ✅ **Надёжность** - продвинутая логика обхода защиты от ботов
- ✅ **REST API** - простой HTTP интерфейс для интеграции
- ✅ **База данных** - автоматический выбор получателей из БД
- ✅ **Playwright** - современная автоматизация браузера

## 🏗️ Архитектура

```
admin/
├── api_server.py              # Flask API сервер
├── payment_service/
│   ├── payment_service.py     # Основной сервис автоматизации
│   ├── payzteam_api.py        # API клиент
│   └── steps/
│       ├── step1_amount.py    # Шаг 1: Ввод суммы
│       ├── step2_form.py      # Шаг 2: Заполнение формы
│       └── form_helpers.py    # Вспомогательные функции
├── database.py                # Работа с БД получателей
└── test_payment_with_beneficiary.py  # Тестовый скрипт
```

## 🚀 Быстрый старт

### Установка зависимостей

```bash
cd admin
pip install -r requirements.txt
pip install -r requirements_playwright.txt
playwright install chromium
```

### Запуск API сервера

```bash
python3 api_server.py
```

Сервер запустится на `http://localhost:5001`

### Создание платежа через API

```bash
curl -X POST http://localhost:5001/api/payment \
  -H "Authorization: Bearer -3uVLlbWyy90eapOGkv70C2ZltaYTxq-HtDbq-DtlLo" \
  -H "Content-Type: application/json" \
  -d '{"amount": 1000, "orderId": "test123"}'
```

**Ответ:**
```json
{
  "success": true,
  "qr_link": "https://qr.nspk.ru/BD100001...",
  "payment_time": 20.7,
  "step1_time": 4.0,
  "step2_time": 16.7,
  "order_id": "test123",
  "status": "completed"
}
```

## 📊 API Endpoints

### POST `/api/payment`
Создание платёжной ссылки

**Headers:**
- `Authorization: Bearer -3uVLlbWyy90eapOGkv70C2ZltaYTxq-HtDbq-DtlLo`
- `Content-Type: application/json`

**Body:**
```json
{
  "amount": 1000,
  "orderId": "unique_order_id"
}
```

### GET `/health`
Проверка статуса сервиса

**Response:**
```json
{
  "status": "ok",
  "service": "LinkFlow API",
  "version": "2.0.0",
  "mode": "playwright",
  "browser_ready": true
}
```

## ⚙️ Конфигурация

### Переменные окружения

Не требуются - все настройки в коде.

### База данных

SQLite база `linkflow.db` содержит таблицу `beneficiaries` с данными получателей:
- `card_number` - номер карты
- `owner_name` - ФИО владельца
- `birth_date` - дата рождения
- `passport_series` - серия паспорта
- `passport_number` - номер паспорта
- `passport_issue_date` - дата выдачи
- `passport_issued_by` - кем выдан

## 🔧 Технические детали

### Headless режим

Система использует продвинутую логику для работы в headless режиме:

1. **30 попыток** закрытия модального окна
2. **6 различных методов** клика (Playwright, force, JS, координаты, Enter, агрессивные JS события)
3. **Циклическое переключение** методов для обхода защиты
4. **Короткие задержки** (300ms) между попытками

### Производительность

- **Общее время:** ~20-30 секунд
- **Шаг 1 (ввод суммы):** ~4-7 секунд
- **Шаг 2 (форма + модалка):** ~16-21 секунд
- **Запуск браузера:** ~11 секунд (один раз)

### Обработка ошибок

Система автоматически определяет ошибки:
- Карта получателя не найдена
- Данные не актуальны
- Некорректные данные
- Таймауты и сетевые ошибки

## 🐳 Деплой на продакшн

### Nginx конфигурация

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location /api/payment {
        proxy_pass http://127.0.0.1:5001/api/payment;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_connect_timeout 120s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }

    location = /api/health {
        proxy_pass http://127.0.0.1:5001/health;
    }
}
```

### Запуск в фоне

```bash
cd /root/LinkFlow/admin
nohup python3 api_server.py > api_server.log 2>&1 &
```

## 🧪 Тестирование

```bash
python3 admin/test_payment_with_beneficiary.py
```

## 📝 Логирование

Все операции логируются с временными метками:
- Начало/завершение каждого шага
- Попытки клика на модальное окно (каждая 5-я)
- Ошибки и предупреждения
- Финальный результат с таймингами

## 🔐 Безопасность

- Bearer токен для авторизации API
- Валидация входных данных
- Защита от SQL инъекций через параметризованные запросы
- Ограничение доступа через nginx

## 📄 Лицензия

Proprietary - все права защищены

## 🤝 Поддержка

Система работает стабильно в headless режиме на продакшн серверах без монитора. Проверено в боевых условиях!

---

**Made with ❤️ for automated payment processing**
