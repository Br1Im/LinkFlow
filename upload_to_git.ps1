# Скрипт для загрузки на GitHub
# Замените YOUR_USERNAME на ваш GitHub username

Write-Host "🚀 Загрузка LinkFlow Payment Bot на GitHub..." -ForegroundColor Green

# Удаляем старый origin если есть
git remote remove origin 2>$null

# Добавляем новый origin (ЗАМЕНИТЕ YOUR_USERNAME!)
$repoUrl = Read-Host "Введите URL вашего GitHub репозитория (например: https://github.com/username/linkflow-payment-bot.git)"

git remote add origin $repoUrl

# Проверяем статус
Write-Host "📊 Статус Git:" -ForegroundColor Yellow
git status

# Добавляем все файлы
Write-Host "📁 Добавляем файлы..." -ForegroundColor Yellow
git add .

# Создаем коммит
Write-Host "💾 Создаем коммит..." -ForegroundColor Yellow
git commit -m "🚀 РАБОЧАЯ ВЕРСИЯ: Ультра-быстрое создание платежей 15-20 секунд

✅ ОСНОВНЫЕ ФУНКЦИИ:
- Прогретый браузер с авторизацией
- Создание платежей за 15-20 секунд
- Админ-панель с темной темой
- Автоматический мониторинг авторизации
- Блокировка конкурентного доступа

⚡ ОПТИМИЗАЦИИ:
- Исправлена проблема с кнопкой Оплатить
- Увеличено время ожидания обработки суммы
- Добавлены строгие проверки готовности
- Подробная диагностика и логирование

🔧 КОМПОНЕНТЫ:
- payment_service_ultra.py: ультра-быстрый сервис
- admin_panel.py: веб-интерфейс с темной темой
- browser_manager.py: управление браузером
- test_payment_speed.py: тестирование скорости"

# Отправляем на GitHub
Write-Host "🌐 Отправляем на GitHub..." -ForegroundColor Yellow
git push -u origin master

if ($LASTEXITCODE -eq 0) {
    Write-Host "✅ УСПЕШНО! Код загружен на GitHub" -ForegroundColor Green
    Write-Host "🔗 Ваш репозиторий: $repoUrl" -ForegroundColor Cyan
} else {
    Write-Host "❌ Ошибка загрузки. Проверьте URL и права доступа." -ForegroundColor Red
}

Write-Host "📋 Для клонирования на сервере используйте:" -ForegroundColor Yellow
Write-Host "git clone $repoUrl" -ForegroundColor White

Read-Host "Нажмите Enter для выхода"