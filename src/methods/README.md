# Методы автоматизации

Эта папка содержит различные способы автоматизации создания платежей.

## Доступные методы

### 1. Selenium (`selenium/`)
**Статус:** ✅ Работает  
**Время:** ~2 минуты (124 сек)  
**Описание:** Использует Selenium WebDriver для автоматизации браузера Chrome

**Файлы:**
- `multitransfer_payment.py` - основной класс для работы с multitransfer.ru
- `mui_helpers.py` - вспомогательные функции для работы с Material-UI

**Использование:**
```python
from src.methods.selenium import MultitransferPayment

payment = MultitransferPayment(headless=True, skip_bank_selection=True)
payment.login()
result = payment.create_payment(
    card_number="9860080323894719",
    owner_name="Nodir Asadullayev",
    amount=500
)
payment.close()
```

**Преимущества:**
- ✅ Работает с любыми сайтами
- ✅ Видит страницу как реальный пользователь
- ✅ Может проходить капчи

**Недостатки:**
- ⚠️ Медленный (~2 минуты)
- ⚠️ Требует Chrome/ChromeDriver
- ⚠️ Потребляет много ресурсов

---

### 2. Playwright (планируется)
**Статус:** 🚧 В разработке  
**Описание:** Современная альтернатива Selenium с лучшей производительностью

---

### 3. Requests + API (планируется)
**Статус:** 🚧 В разработке  
**Описание:** Прямые HTTP запросы к API сайта (если доступно)

**Преимущества:**
- ⚡ Очень быстро (~5-10 сек)
- 💪 Минимум ресурсов
- 🎯 Надёжно

**Недостатки:**
- ⚠️ Требует reverse engineering API
- ⚠️ Может не работать с капчей

---

### 4. Puppeteer/Pyppeteer (планируется)
**Статус:** 🚧 В разработке  
**Описание:** Headless Chrome через CDP протокол

---

## Как добавить новый метод

1. Создайте папку `src/methods/your_method/`
2. Добавьте файлы с реализацией
3. Создайте `__init__.py` с экспортами
4. Обновите этот README
5. Добавьте тесты

## Выбор метода

Используйте переменную окружения или параметр:

```python
# В admin/app.py
payment_method = os.getenv('PAYMENT_METHOD', 'selenium')

if payment_method == 'selenium':
    from src.methods.selenium import MultitransferPayment
elif payment_method == 'playwright':
    from src.methods.playwright import MultitransferPayment
# и т.д.
```
