# 🚀 Быстрый старт LinkFlow

## Запуск через Docker (рекомендуется)

### 1. Запустить админ-панель

```bash
cd LinkFlow
./start.sh
```

### 2. Открыть в браузере

```
http://localhost:5000
```

### 3. Создать платёж

1. Выберите платёжную систему (Multitransfer или Elecsnet)
2. Выберите режим платёжки (standard/fast/test)
3. Введите номер карты получателя (16 цифр)
4. Введите имя владельца карты (латиницей)
5. Укажите сумму (100-75000 RUB)
6. Нажмите "Создать платёж"

Платёж создастся за ~40-60 секунд.

## Тестирование через API

```bash
./test_api.sh
```

Этот скрипт:
- Проверит доступность админки
- Создаст тестовый платёж
- Будет проверять статус каждые 3 секунды
- Покажет результат

## Ручное тестирование API

### Создать платёж

```bash
curl -X POST http://localhost:5000/api/create-payment \
  -H "Content-Type: application/json" \
  -d '{
    "card_number": "9860080323894719",
    "owner_name": "Test User",
    "amount": 500,
    "payment_mode": "test",
    "payment_system": "multitransfer"
  }'
```

Ответ:
```json
{
  "success": true,
  "payment_id": 1,
  "message": "Платеж создается..."
}
```

### Проверить статус

```bash
curl http://localhost:5000/api/payment/1
```

Ответ:
```json
{
  "id": 1,
  "card_number": "9860080323894719",
  "owner_name": "Test User",
  "amount": 500,
  "payment_mode": "test",
  "payment_system": "multitransfer",
  "status": "completed",
  "result": {
    "success": true,
    "payment_link": "https://...",
    "elapsed_time": 45.2
  }
}
```

### Список всех платежей

```bash
curl http://localhost:5000/api/payments
```

## Режимы платежей

| Режим | Лимиты | Описание |
|-------|--------|----------|
| **standard** | 100-75000 RUB | Обычный режим |
| **fast** | 100-15000 RUB | Быстрый режим |
| **test** | 100-1000 RUB | Тестовый режим |

## Платёжные системы

- **multitransfer** - Multitransfer.ru
- **elecsnet** - Elecsnet.ru

## Остановка

```bash
docker-compose -f docker-compose.local.yml down
```

## Логи

```bash
docker-compose -f docker-compose.local.yml logs -f
```

## Проблемы?

См. [docs/TROUBLESHOOTING.md](docs/TROUBLESHOOTING.md)
