# 🚀 Быстрый старт LinkFlow с Docker

## 1. Запуск

```bash
docker-compose up --build
```

Или в фоновом режиме:

```bash
docker-compose up -d --build
```

## 2. Доступ

- **Админ-панель**: http://localhost:5000
- **API**: http://localhost:5001

## 3. Что происходит автоматически

✅ Устанавливается Python и все зависимости  
✅ Устанавливается Playwright и Chromium  
✅ Создается база данных SQLite  
✅ Импортируются данные из `100.xlsx` (100 записей)  
✅ Запускается админ-панель (порт 5000)  
✅ Запускается API сервер (порт 5001)  

## 4. Использование

### Через админ-панель

1. Откройте http://localhost:5000
2. Введите сумму и нажмите "Создать платеж"
3. Скрипт автоматически:
   - Выберет случайные данные отправителя из БД
   - Заполнит форму на multitransfer.ru
   - Решит капчу
   - Вернет QR-ссылку

### Через API

```bash
curl -X POST http://localhost:5001/api/payment \
  -H "Authorization: Bearer -3uVLlbWyy90eapOGkv70C2ZltaYTxq-HtDbq-DtlLo" \
  -H "Content-Type: application/json" \
  -d '{"amount": 1000, "orderId": "TEST-001"}'
```

## 5. Управление данными

### Просмотр данных в БД

```bash
# Войти в контейнер
docker-compose exec linkflow-admin bash

# Открыть БД
sqlite3 admin/linkflow.db

# Посмотреть данные отправителей
SELECT * FROM sender_data LIMIT 5;

# Выйти
.exit
```

### Добавление новых данных

Через админ-панель будет раздел "Данные отправителей" где можно:
- Добавлять новые записи
- Редактировать существующие
- Активировать/деактивировать

## 6. Остановка

```bash
docker-compose down
```

## 7. Логи

```bash
# Все логи
docker-compose logs

# Следить в реальном времени
docker-compose logs -f
```

## 8. Перезапуск

```bash
docker-compose restart
```

## Готово! 🎉

Система полностью автоматизирована:
- ✅ Данные хранятся в БД
- ✅ Скрипт берет случайные данные при каждом платеже
- ✅ Админка позволяет управлять всем
- ✅ API готов к интеграции
