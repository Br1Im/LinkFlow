# 🤝 Руководство по разработке

## 🚀 Начало работы

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd LinkFlow
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Запуск тестов

#### Локально
```bash
python3 -m tests.test_local
```

#### Docker
```bash
cd scripts
./run_test.sh
```

## 📁 Структура проекта

- `src/` - исходный код
- `tests/` - тесты
- `scripts/` - скрипты запуска
- `docs/` - документация
- `screenshots/` - скриншоты для отладки

## 🔧 Разработка

### Добавление новой функции

1. Создайте ветку: `git checkout -b feature/new-feature`
2. Внесите изменения в `src/`
3. Добавьте тесты в `tests/`
4. Обновите документацию в `docs/`
5. Запустите тесты
6. Создайте Pull Request

### Стиль кода

- Используйте PEP 8
- Добавляйте docstrings
- Комментируйте сложную логику
- Используйте type hints где возможно

### Коммиты

Формат: `<type>: <description>`

Типы:
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - документация
- `test:` - тесты
- `refactor:` - рефакторинг
- `style:` - форматирование

Примеры:
```
feat: добавлен выбор банка
fix: исправлено ожидание списка банков
docs: обновлена архитектура
```

## 🧪 Тестирование

### Запуск всех тестов

```bash
# Локально
python3 -m tests.test_local

# Docker
cd scripts && ./run_test.sh
```

### Отладка

1. Используйте `test_local.py` для видимого браузера
2. Проверяйте скриншоты в `screenshots/`
3. Смотрите логи в консоли

## 📝 Документация

При добавлении функций обновляйте:
- `README.md` - основное описание
- `docs/README.md` - детальная документация
- `docs/ARCHITECTURE.md` - архитектура
- Docstrings в коде

## 🐛 Отчеты об ошибках

Включите:
- Описание проблемы
- Шаги воспроизведения
- Ожидаемое поведение
- Фактическое поведение
- Скриншоты (если есть)
- Версия Python
- ОС

## 💡 Предложения

Создайте Issue с:
- Описанием функции
- Примерами использования
- Обоснованием необходимости

## 📦 Релизы

1. Обновите версию в `setup.py`
2. Обновите `CHANGELOG.md`
3. Создайте тег: `git tag v1.0.0`
4. Push: `git push --tags`

## 🎯 Приоритеты

1. Стабильность
2. Производительность
3. Документация
4. Новые функции

## ✅ Checklist перед PR

- [ ] Код работает локально
- [ ] Код работает в Docker
- [ ] Тесты проходят
- [ ] Документация обновлена
- [ ] Нет лишних файлов
- [ ] Коммиты осмысленные
- [ ] Код отформатирован

## 🙏 Спасибо!

Спасибо за вклад в проект!
