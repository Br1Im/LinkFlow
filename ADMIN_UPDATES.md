# Обновления админ-панели

## Что добавлено

### 1. Тёмная тема по умолчанию 🌙
- **Тёмная тема включена по умолчанию** при первом открытии
- Переключатель темы находится в разделе **Настройки**
- Автоматическое сохранение выбранной темы в localStorage
- Плавные переходы между темами
- Адаптивные цвета для всех элементов интерфейса

**Как использовать:**
1. Откройте раздел "Настройки" в боковом меню
2. В карточке "Внешний вид" переключите тумблер "Тёмная тема"
3. Тема автоматически сохранится и будет применяться при следующем посещении

### 2. Убрана опция ручного прогрева браузера
- Браузер теперь **всегда** прогревается автоматически при запуске системы
- Удалена кнопка "Прогреть браузер" из настроек
- В разделе "Настройки" теперь только информация о системе и переключатель темы

**Почему это важно:**
- Гарантирует, что браузер всегда готов к работе
- Исключает ситуации, когда пользователь забыл прогреть браузер
- Обеспечивает стабильную скорость создания платежей (8-12 секунд)

## Технические детали

### CSS переменные для тем
```css
:root {
    --bg-primary: #f8f9fa;      /* Светлая тема */
    --text-primary: #212529;
}

[data-theme="dark"] {
    --bg-primary: #1a1a1a;      /* Тёмная тема */
    --text-primary: #e9ecef;
}
```

### Сохранение темы
```javascript
localStorage.setItem('theme', 'dark');  // Сохранение
const theme = localStorage.getItem('theme') || 'dark';  // Загрузка (тёмная по умолчанию)
```

### Переключатель в настройках
Красивый тумблер-переключатель в разделе "Настройки" → "Внешний вид"

## Доступ к админке

**URL:** http://localhost:5001

**Функции:**
- ✅ Создание платежных ссылок
- ✅ Просмотр всех платежей
- ✅ Управление аккаунтами
- ✅ Управление картами
- ✅ Переключение темы
- ✅ Детальное логирование всех запросов

## Автоматический прогрев

При запуске Docker контейнера:
1. Система автоматически запускает прогрев браузера
2. Браузер готов к работе через ~14 секунд
3. Все последующие платежи создаются за 8-12 секунд

Логи прогрева:
```
🔥 Запуск автоматического прогрева браузера...
🔧 Прогрев в SELENIUM режиме...
✅ Браузер прогрет за 14.4s
```
